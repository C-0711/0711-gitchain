{
  "name": "@0711/git",
  "version": "0.1.0",
  "description": "Git versioning layer for GitChain containers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "isomorphic-git": "^1.25.0",
    "@0711/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
