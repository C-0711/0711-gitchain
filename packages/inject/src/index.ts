/**
 * @0711/inject - Context injection for AI agents
 * 
 * The core GitChain innovation: verified context for any AI agent.
 * No hallucination. Full audit. Verified context.
 */

export { inject } from "./injector";
export { formatForLLM } from "./formatter";
export { verifyProofs } from "./verifier";
export { resolveContainers } from "./resolver";
export type { InjectOptions, InjectedContext } from "./types";
